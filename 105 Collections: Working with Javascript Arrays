var guestList = ["Angela","Jack","Pam","James","Lara", "Jason"];
var guestName = prompt("What's your name, please?");
    if (guestList.includes(guestName)){
        alert("Welcome "+guestName+"!");
    }else{
        alert("Sorry, maybe next time!");
    }
